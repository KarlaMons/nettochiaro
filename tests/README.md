# Tests

Vitest will cover formula boundaries, rounding, reconciliation, validation, and components. Playwright flows will live in root-level `e2e/` and cover calculation, explanations, sources, keyboard use, and mobile layout.

The EUR 30,000 reference case in `design/calculation_rules.md` is a mandatory regression test.
