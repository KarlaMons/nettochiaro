# Deployment Scripts

No deployment script is currently required. EasyPanel handles build, deploy, health monitoring, and rollback for the single stateless container.

If automation is added later, limit it to reproducible smoke or health checks; NettoChiaro has no seed data or migrations.
