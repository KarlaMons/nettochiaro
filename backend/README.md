# Backend

NettoChiaro has no separate backend service. The approved Next.js application exposes only `GET /api/health`; salary calculation runs through pure TypeScript modules in the browser.

This template directory is retained until IT-01 scaffolds the application and cleans up obsolete placeholders.
