# Development container orchestration
# Services are defined during planning based on stack selection

services:
  # backend:
  #   build: ./backend
  #   ports:
  #     - "${PORT:-3000}:3000"
  #   env_file: .env
  #   depends_on:
  #     - db

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "5173:5173"

  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: app
  #     POSTGRES_USER: app
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data

# volumes:
#   db_data:
