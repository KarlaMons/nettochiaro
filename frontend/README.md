# Frontend

The approved architecture uses root-level Next.js `app/` and `components/` directories rather than a separate frontend service.

This template directory is retained until IT-01 scaffolds the application and cleans up obsolete placeholders.
